class URL {
	private root = '/wb'

	HOME = this.root
	SUPPLIES = `${this.root}/supplies`
	PRODUCTS = `${this.root}/products`
	SALE = `${this.root}/sale`
	ANALYTICS = `${this.root}/analytics`
	MARKETING = `${this.root}/marketing`
}

export const URL_PAGES = new URL()


